server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:milkytea;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password: ""
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: false

logging:
  level:
    org.springframework.security: INFO

app:
  jwt:
    secret: "change-this-dev-secret-key-change-this-dev-secret-key-change-this"
    expirationMs: 604800000 # 7 days

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    path: /swagger-ui.html

