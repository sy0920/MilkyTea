# MilkyTea 🧋
今天你喝奶茶了吗

一个用于记录每天喝的奶茶的Web应用，可以记录奶茶的品牌、品类、甜度、冰度、价格、评分和评语，并提供统计功能。

## 技术栈

### 后端
- **Spring Boot 2.7.14** - Java后端框架
- **Spring Data JPA** - 数据持久化
- **H2 Database** - 嵌入式数据库
- **Maven** - 项目管理工具

### 前端
- **React 18** - 前端框架
- **React Router** - 路由管理
- **Axios** - HTTP客户端

## 功能特性

1. **添加奶茶记录**
   - 品牌（如：喜茶、奈雪的茶）
   - 品类（如：芝士奶盖、水果茶）
   - 甜度（无糖、三分糖、五分糖、七分糖、全糖）
   - 冰度（去冰、少冰、正常冰、多冰）
   - 价格
   - 评分（1-10分）
   - 评语（可选）

2. **查看记录列表**
   - 按时间倒序显示所有记录
   - 卡片式展示，信息清晰
   - 支持删除记录

3. **统计分析**
   - 总共喝了多少杯奶茶
   - 最近7天喝了多少杯
   - 总花费金额
   - 平均评分
   - 最爱品牌
   - 消费分析

## 安装和运行

### 前置要求
- Java 11 或更高版本
- Maven 3.6+
- Node.js 14+ 和 npm

### 后端设置

1. 进入后端目录：
```bash
cd backend
```

2. 构建项目：
```bash
mvn clean package
```

3. 运行后端服务：
```bash
java -jar target/milkytea-backend-1.0.0.jar
```

后端将在 `http://localhost:8080` 运行

### 前端设置

1. 进入前端目录：
```bash
cd frontend
```

2. 安装依赖：
```bash
npm install
```

3. 运行前端开发服务器：
```bash
npm start
```

前端将在 `http://localhost:3000` 运行

## API接口

### 奶茶记录接口

- `GET /api/tea-records` - 获取所有记录
- `GET /api/tea-records/{id}` - 获取单条记录
- `POST /api/tea-records` - 创建新记录
- `PUT /api/tea-records/{id}` - 更新记录
- `DELETE /api/tea-records/{id}` - 删除记录
- `GET /api/tea-records/statistics` - 获取统计数据

## 数据库

项目使用H2嵌入式数据库，数据将保存在 `backend/data` 目录下。

如需查看数据库内容，可访问 H2 Console：
- URL: `http://localhost:8080/h2-console`
- JDBC URL: `jdbc:h2:file:./data/milkytea`
- 用户名: `sa`
- 密码: （留空）

## 项目结构

```
MilkyTea/
├── backend/                    # Spring Boot后端
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/
│   │   │   │   └── com/milkytea/app/
│   │   │   │       ├── MilkyTeaApplication.java
│   │   │   │       ├── controller/
│   │   │   │       ├── model/
│   │   │   │       ├── repository/
│   │   │   │       └── dto/
│   │   │   └── resources/
│   │   │       └── application.properties
│   │   └── test/
│   └── pom.xml
├── frontend/                   # React前端
│   ├── src/
│   │   ├── components/
│   │   │   ├── TeaRecordForm.js
│   │   │   ├── TeaRecordList.js
│   │   │   └── Statistics.js
│   │   ├── services/
│   │   │   └── api.js
│   │   ├── App.js
│   │   └── App.css
│   └── package.json
└── README.md
```

## 许可证

MIT License

## 作者

sy0920

